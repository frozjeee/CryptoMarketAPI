[flake8]
per-file-ignores =
    # imported but unused, unable to detect undefined names
    __init__.py: F401, F403

inline-quotes = "
max-line-length = 120
exclude = */alembic/*,docs, venv/*

# check linter rule comment
